#include "local.hpp"

//============================================================================
// TextChunk(text)
//
NFTM::TextChunk::TextChunk(char *text_, int length_) : Chunk(text_, length_) {
}
