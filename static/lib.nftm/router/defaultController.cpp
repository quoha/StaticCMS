#include "local.hpp"

//============================================================================
// DefaultController(controller)
//
void NFTM::Router::DefaultController(NFTM::Controller *controller) {
	defaultController = controller;
}
